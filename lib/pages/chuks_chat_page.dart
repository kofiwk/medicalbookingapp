import 'package:flutter/material.dart';

class ChuksPage extends StatelessWidget {
  const ChuksPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}